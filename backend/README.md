# Mistral task backend
Run `npm install` then `npm start` to get up and running

## Note

Connect to a mongodb database first and seed the data using postman and doing a 
GET request to `/movies/seed `